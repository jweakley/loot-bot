<%= form_for(@raid_boss) do |f| %>
  <% if @raid_boss.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@raid_boss.errors.count, "error") %> prohibited this raid_boss from being saved:</h2>

      <ul>
      <% @raid_boss.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
